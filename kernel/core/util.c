#include <core/util.h>
#include <core/types.h>

#include <algorithm>

inline int gcd(int a, int b)
{
    return __gcd(a, b);
}

uint64_t gcd_weight(){
    return 0;
}

uint64_t max_weight(){
    return 0;
}